= form_for @flight_type do |f|
  - if @flight_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@flight_type.errors.count, "error")} prohibited this flight_type from being saved:"
      %ul
        - @flight_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :flight_type
    = f.text_field :flight_type
  .actions
    = f.submit 'Save'
